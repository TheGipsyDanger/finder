import { call, put } from 'redux-saga/effects'
import { Actions as {{pascalCase name }}Actions }from '../reducers/{{pascalCase name }}'
import { navigat, API } from '../../utils'

export function* init() {
try {
} catch (err) {}
}